<ul>
<?php foreach ($this->container as $page) : ?>
    <li class="<?= $page->isActive(true) ? 'current' : '' ?>">
        <a href="<?= $page->getHref()?>"><?= $page->label ?></a>
    </li>
<?php endforeach ?>
</ul>
