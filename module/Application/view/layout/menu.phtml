<ul>
    <?php
    foreach ($this->container as $page) :
    ?>
        <li class="<?php echo $page->isActive(true) ? 'current' : ''?>">
            <a href="<?php echo $page->getHref()?>"><?php echo $page->label?></a>
        </li>
    <?php endforeach; ?>
</ul>